# Artifacts

M0 does not require benchmark artifacts.

Future public reproduction artifacts must be small, public, and tied to an approved protocol. Do not store profiler HTML, raw stack traces, JFR files, heap dumps, large logs, database dumps, user absolute paths, secrets, private artifacts, or private source information here.
