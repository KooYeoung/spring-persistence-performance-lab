# Results

M0 does not publish official benchmark results.

Future public reproduction results may include small summary files, elapsed times, environment versions, row counts, checksums, and median calculations after the relevant protocol is executed.

Do not store profiler HTML, raw stack traces, JFR files, heap dumps, large logs, database dumps, user absolute paths, secrets, private artifacts, or private source information here.
